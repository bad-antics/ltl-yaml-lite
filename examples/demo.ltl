// examples/demo.ltl — runnable demo for ltl_yaml_lite

import std::io
use ltl_yaml_lite as lib

fn main() {
    io::println("ltl_yaml_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}